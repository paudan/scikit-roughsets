language: python
python:
  - "2.7"
  - "3.3"
  - "3.4"
  - "3.5"
  - "3.6"
addons:
  apt:
    packages:
      - scikit-learn
install:
  - python setup.py -q install
script: nosetests -s -v --with-coverage --cover-package=scikit_roughsets --cover-html
cache: pip
sudo: false